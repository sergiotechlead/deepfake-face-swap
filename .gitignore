# Downloaded swap model and insightface's own cached models
models/
.insightface/

# GFPGAN's own auxiliary weights (face detector, parsing net), downloaded on first
# `--enhance` use to a path it hardcodes relative to the working directory
gfpgan/

# Generated output
output/*
!output/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
venv/

# npm (package.json only wraps the Python CLI; no actual Node deps)
node_modules/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Claude Code local state
.claude/
